{
  "$schema": "http://json-schema.org/schema",
  "$id": "PublishExecutorOptions",
  "title": "A executor to publish NPM packages",
  "type": "object",
  "properties": {
    "printConfig": {
      "type": "boolean",
      "description": "Print the commands that would be run, but don't actually run them",
      "default": false
    },
    "environmentRoot": {
      "type": "string",
      "description": "The root folder of the environment",
      "aliases": ["envRoot", "e"]
    },
    "releaseTarget": {
      "type": "string",
      "description": "The target to run to release the package",
      "default": "build"
    },
    "optionsOutputPathKey": {
      "type": "string",
      "description": "The key in options specifying the output path of the build target",
      "default": "outputPath"
    },
    "verbose": {
      "type": "boolean",
      "description": "Print additional logs"
    }
  },
  "additionalProperties": true
}
